{
  "name": "@kaspacom/bridge-service",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "npm run typecheck && node --test test/*.test.ts"
  }
}
